{{!-- display all blogs--}}
  <section class="container thumbnail-container">
    <div class="row">
        {{#each blogList as |blog|}}
        {{> blog-list}}
        {{/each}}

{{#if session.loggedIn}}

<script src="/js/log-out.js"></script>


{{/if}}